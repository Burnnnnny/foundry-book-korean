## `skip`

### Signature

```solidity
function skip(uint256 time) public;
```

### Description

지정된 초만큼 `block.timestamp`를 앞으로 이동시킵니다.

### Examples

```solidity
assertEq(block.timestamp, 0);
skip(3600);
assertEq(block.timestamp, 3600);
```
