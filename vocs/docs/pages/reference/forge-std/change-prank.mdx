## `changePrank`

### Status

`changePrank`는 더 이상 사용되지 않습니다(deprecated).

### Signature

```solidity
function changePrank(address who) internal;
```

### Description

`stopPrank`로 활성 prank를 중지하고 주소를 `startPrank`에 전달합니다.  

`setUp` 함수에서 전역 prank를 시작하고 특정 테스트에서 비활성화할 때 유용합니다.
