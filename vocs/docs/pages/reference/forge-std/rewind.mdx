## `rewind`

### Signature

```solidity
function rewind(uint256 time) public;
```

### Description

지정된 초만큼 `block.timestamp`를 되감습니다.

### Examples

```solidity
assertEq(block.timestamp, 3600);
rewind(3600);
assertEq(block.timestamp, 0);
```
