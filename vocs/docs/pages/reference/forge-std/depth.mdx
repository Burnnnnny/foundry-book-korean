## `depth`

### Signature

```solidity
function depth(StdStorage storage self, uint256 _depth) internal returns (StdStorage storage);
```

### Description

`tuple`(예: `struct` 내부)에서 값의 위치를 설정합니다.

기본값: `uint256(0)`
