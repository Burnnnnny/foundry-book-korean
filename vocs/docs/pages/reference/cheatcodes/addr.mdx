## `addr`

### 서명

```solidity
function addr(uint256 privateKey) external returns (address);
```

### 설명

주어진 개인 키(private key)에 대한 주소를 계산합니다.

### 예제

```solidity
address alice = vm.addr(1);
emit log_address(alice); // 0x7e5f4552091a69125d5dfcb7b8c2659029395bdf
```
