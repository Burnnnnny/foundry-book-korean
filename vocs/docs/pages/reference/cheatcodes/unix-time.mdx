## `unixTime`

### 시그니처

```solidity
function unixTime() external returns (uint milliseconds);
```

### 설명

유닉스 에포크(unix epoch) 이후의 시간을 밀리초 단위로 반환합니다.

### 예제

```solidity
uint start = vm.unixTime();
vm.sleep(10_000); // 10초 동안 실행 중지
uint end = vm.unixTime();
assertEq(end - start, 10_000);
```
