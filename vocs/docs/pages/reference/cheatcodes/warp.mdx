## `warp`

### 시그니처

```solidity
function warp(uint256) external;
```

### 설명

`block.timestamp`를 설정합니다.

### 예제

```solidity
vm.warp(1641070800);
emit log_uint(block.timestamp); // 1641070800
```

### 같이 보기

Forge 표준 라이브러리

[`skip`](/reference/forge-std/skip.mdx), [`rewind`](/reference/forge-std/rewind.mdx)
