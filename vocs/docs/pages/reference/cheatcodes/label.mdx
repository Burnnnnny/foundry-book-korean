## `label`

### 시그니처

```solidity
function label(address addr, string calldata label) external;
```

### 설명

`addr`에 대해 테스트 트레이스(trace)에 표시될 라벨 `label`을 설정합니다.

주소에 라벨이 지정되면 테스트 트레이스에서 주소 대신 라벨이 표시됩니다.
