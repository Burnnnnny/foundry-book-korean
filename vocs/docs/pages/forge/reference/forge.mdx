## forge

Solidity 컨트랙트를 빌드, 테스트, 퍼즈, 디버그 및 배포합니다.

```bash
$ forge --help
```

```txt
Usage: forge [OPTIONS] <COMMAND>

Commands:
  bind             스마트 컨트랙트를 위한 Rust 바인딩 생성
  bind-json        JSON 치트코드를 통한 프로젝트 구조체 직렬화/역직렬화를 위한 바인딩 생성
  build            프로젝트의 스마트 컨트랙트 빌드 [aliases: b, compile]
  cache            Foundry 캐시 관리
  clean            빌드 아티팩트 및 캐시 디렉토리 제거 [aliases:
                   cl]
  clone            Etherscan에서 컨트랙트 복제
  compiler         컴파일러 유틸리티
  completions      쉘 자동 완성 스크립트 생성 [aliases: com]
  config           현재 구성 표시 [aliases: co]
  coverage         커버리지 보고서 생성
  create           스마트 컨트랙트 배포 [aliases: c]
  doc              프로젝트 문서 생성
  eip712           주어진 파일의 구조체에 대한 EIP-712 구조체 인코딩 생성
  flatten          소스 파일과 모든 임포트를 하나의 파일로 병합(Flatten)
                   [aliases: f]
  fmt              Solidity 소스 파일 포맷팅
  geiger           DEPRECATED: 프로젝트 및 종속성에서 안전하지 않은 치트 코드 사용 감지
  help             이 메시지 또는 주어진 하위 명령의 도움말 인쇄
  init             새로운 Forge 프로젝트 생성
  inspect          스마트 컨트랙트에 대한 전문 정보 가져오기 [aliases:
                   in]
  install          하나 이상의 종속성 설치 [aliases: i, add]
  lint             Solidity 소스 파일 린트 [aliases: l]
  remappings       프로젝트에 대해 자동으로 추론된 리매핑 가져오기
                   [aliases: re]
  remove           하나 이상의 종속성 제거 [aliases: rm]
  script           스마트 컨트랙트를 스크립트로 실행하여 온체인으로 보낼 수 있는 트랜잭션 구축
  selectors        함수 선택자(Selector) 유틸리티 [aliases: se]
  snapshot         각 테스트의 가스 사용량에 대한 가스 스냅샷 생성 [aliases: s]
  soldeer          Soldeer 종속성 관리자
  test             프로젝트 테스트 실행 [aliases: t]
  tree             프로젝트 종속성 그래프의 트리 시각화 표시
                   [aliases: tr]
  update           하나 이상의 종속성 업데이트 [aliases: u]
  verify-bytecode  Etherscan에서 배포된 바이트코드를 소스에 대해 검증
                   [aliases: vb]
  verify-check     Etherscan에서 검증 상태 확인 [aliases: vc]
  verify-contract  Etherscan에서 스마트 컨트랙트 검증 [aliases: v]

Options:
  -h, --help
          도움말 인쇄 ('-h'로 요약 보기)

  -j, --threads <THREADS>
          사용할 스레드 수. 0으로 지정하면 기본값은 논리 코어 수
          
          [aliases: --jobs]

  -V, --version
          버전 인쇄

Display options:
      --color <COLOR>
          로그 메시지의 색상

          가능한 값:
          - auto:   색상 출력 사용 여부를 지능적으로 추측 (기본값)
          - always: 색상 출력 강제
          - never:  색상 출력 강제 비활성화

      --json
          로그 메시지를 JSON으로 포맷팅

      --md
          로그 메시지를 Markdown으로 포맷팅

  -q, --quiet
          로그 메시지를 인쇄하지 않음

  -v, --verbosity...
          로그 메시지의 상세 수준.
          
          상세 수준을 높이려면 여러 번 전달 (예: -v, -vv, -vvv).
          
          문맥에 따라 상세 수준은 다른 의미를 가짐.
          
          예를 들어, EVM의 상세 수준은 다음과 같음:
          - 2 (-vv): 모든 테스트에 대한 로그 인쇄.
          - 3 (-vvv): 실패한 테스트에 대한 실행 트레이스 인쇄.
          - 4 (-vvvv): 모든 테스트에 대한 실행 트레이스 및 실패한 테스트에 대한 설정(setup) 트레이스 인쇄.
          - 5 (-vvvvv): 스토리지 변경 및 줄 번호가 포함된 백트레이스를 포함하여
            모든 테스트에 대한 실행 및 설정 트레이스 인쇄.

Find more information in the book: https://getfoundry.sh/forge/overview
```